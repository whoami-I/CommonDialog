<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--  caution here!!!,the animation of window must define here,not in R.anim.-->

    <style name="anim_bottom_in">
        <item name="android:windowEnterAnimation">@anim/anim_bottm_in</item>
        <item name="android:windowExitAnimation">@anim/anim_bottm_out</item>
    </style>

    <style name="anim_left_in">
        <item name="android:windowEnterAnimation">@anim/anim_left_in</item>
        <item name="android:windowExitAnimation">@anim/anim_left_out</item>
    </style>

    <style name="anim_right_in">
        <item name="android:windowEnterAnimation">@anim/anim_right_in</item>
        <item name="android:windowExitAnimation">@anim/anim_right_out</item>
    </style>
</resources>
